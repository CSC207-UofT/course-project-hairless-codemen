Class name: UserStorage
Parent class: Storage

Responsibilities:
* Store all users created on the service platform.
* When we call the method in UserStorage, we can:
  - getUser (return the user stored on the platform with give username)
  - getUserList(return all users stored on UserStorage)
  - addElement (add user into user storage)
  - deleteElement (after deleting an account, delete the corresponding user from the user storage)
  - getTotalNumber(return the number of users in the UserStorage)
Collaborators:
- User
