Class name: User
Parent class: None

Responsibilities:
* It's a class that stores information about a user.
* When we call the string of the user , it would return the user name.
* When we call methods in this class, we can:
    - changeName (user can chose their own name)
    - setPassword (user can set a new password)
    - getName(return the user's name)
    - getWallet(return the user's wallet)
    - getCart (return the user's cart)
    - getId (return the user's unique id)
    - getPassword(return the user's password)

Collaborators:
-Wallet
-Cart